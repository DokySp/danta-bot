services:
  kis-trade-mcp:
    image: dokysp/kis-trade-mcp:1.0.0
    container_name: kis-trade-mcp
    restart: unless-stopped
    env_file:
      - ./config/kis-trade-mcp.env

networks:
  default:
    name: danta-bot-net
