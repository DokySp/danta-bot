services:
  codex-exec-v2:
    container_name: codex-exec-v2
    image: dokysp/codex-exec-v2:2.1.0
    environment:
      TELEGRAM_ROUTE: v2
    env_file:
      - ./config/codex-exec.env
    restart: unless-stopped
    ports:
      - "127.0.0.1:8091:8080"
    volumes:
      - codex-home-v2:/codex-home
      - codex-state-v2:/state
      - ./config/schedules.yaml:/app/configs/schedules.yaml:ro
      - ./:/workspace

volumes:
  codex-home-v2:
  codex-state-v2:

networks:
  default:
    name: danta-bot-net
