interface:
  display_name: "Check Portfolio"
  short_description: "Read portfolio universe JSON"
  default_prompt: "Use $check-portfolio to read portfolio JSON with recommanded, specified, holding, and universe fields. Use direct KIS holdings from the skill output; the scheduled daily-trading pipeline must not re-read holdings only to expand the universe."
