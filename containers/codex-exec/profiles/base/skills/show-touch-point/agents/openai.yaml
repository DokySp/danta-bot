name: show-touch-point
short_description: "Render trigger points on candles"
default_prompt: "Use /show-touch-point {trigger_id} to render codex-exec touch_log_file events on the matching 30-minute indicator candlestick chart."
