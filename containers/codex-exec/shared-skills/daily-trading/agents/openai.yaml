interface:
  display_name: "Daily Trading"
  short_description: "One-pass portfolio collection, account snapshots, verdicts, and trading"
  default_prompt: "Use $daily-trading, preserve price-only eligible symbols, reuse valid optional caches when available, create compact verdict specs and call only the verified daily-trading launcher run-group/run-one commands for selected 3 functional first-verdict analysts and single judge-final second-verdict, keep verdict sub-agents JSON-only, use telegram-summary.txt as the final Telegram response, and for authorized demo/real submission pass --submit-orders with the default --order-path auto unless the user explicitly requests reservation or immediate so scripts/execute_orders.py owns read-only order gate refresh and order execution."
