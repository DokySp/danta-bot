interface:
  display_name: "Daily Trading"
  short_description: "One-pass portfolio collection, verdicts, and trading"
  default_prompt: "Use $daily-trading to collect the complete portfolio once, run first and second verdicts, persist run artifacts, and only submit explicitly authorized orders."
