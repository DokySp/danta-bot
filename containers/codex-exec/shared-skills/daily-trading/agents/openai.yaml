interface:
  display_name: "Daily Trading"
  short_description: "One-pass portfolio collection, account snapshots, verdicts, and trading"
  default_prompt: "Use $daily-trading, preserve price-only eligible symbols, reuse valid optional caches when available, create compact verdict specs and call only the verified daily-trading launcher run-group/run-one commands for selected 3 functional first-verdict analysts and single judge-midterm second-verdict, keep verdict sub-agents JSON-only, and keep account refresh, order preparation, and any authorized submission inside the Main agent order-execution stage."
